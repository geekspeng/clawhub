FROM oven/bun:1.3.10 AS builder

WORKDIR /app

# 复制源代码
COPY . .

# 安装依赖
RUN bun install --frozen-lockfile

# 暴露端口
EXPOSE 3000

# 开发模式：Vite 实时读取环境变量，修改 .env.local 后重启容器即可
CMD ["bun", "run", "dev", "--", "--host", "0.0.0.0"]